#ifndef UTILS_H
#define UTILS_H

#include<vector>
#include<string>

//double magnitude(std::vector<double> values);

double randomvalue(double lower, double upper);

std::vector<double> randomvector(int Nvals, double lower, double upper);

//double innerProduct(std::vector<double> v1, std::vector<double> v2);

#endif